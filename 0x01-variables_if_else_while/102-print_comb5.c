#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */

int main(void)
{
	int i;
	int j;
	int k;
	int l;
	for (i = '0'; i <= '9'; i++)
	{
		for (j = i + 1; j <= '9'; j++)
		{
			for (k = j + 1; k <= '9'; k++)vc
			{
				for (l = k + 1; k <= '9'; k++)
				{
				putchar(i);
				putchar(j);
				putchar(k);
				putchar(l);
				}
			}
		}
	}
}
						
