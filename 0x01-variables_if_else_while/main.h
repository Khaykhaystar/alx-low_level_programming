#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - A program to print  positive or negative number
 * @Description: to identify when it is a positive value or a negative value
 *
 * Return: Always 0
 */

	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;

	return (0);

