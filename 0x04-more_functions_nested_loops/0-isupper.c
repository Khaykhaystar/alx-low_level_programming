#include "main.h"

/**
 * main - determine if the character is uppercase
 * @c: return 1 if c is upper
 * Return: Always 0
 */


int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
		else
		{
			return (0);
		}
}

